import React, { useContext } from "react";
import styles from "../../components/styles/new.module.css";

export default function shop() {
  return (
    <section className={styles["new-shop"]}>
        <h1>New Shop</h1>
    </section>
  );
}